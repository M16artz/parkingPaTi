# Tareas de fase 002

> **Fase completada el 2026-07-12:** dependencia exceptuada explícitamente,
> decisiones resueltas y criterios verificados. Ver evidencia de la fase.

- [x] `docs` Resolver DP-02, DP-03, DP-10, DP-13 y DP-14. **Evidencia:** decisiones registradas con fecha y responsable en el documento canónico.
- [x] `backend` Implementar Persona-Cuenta OneToOne e identidad/estados.
- [x] `backend` Implementar Cuenta-Parqueadero y Cuenta-Documento OneToOne.
- [x] `backend` Implementar VerificacionCorreo y estados de habilitación.
- [x] `backend` Consolidar Tarifa y retirar fuentes duplicadas con transición segura.
- [x] `backend` Añadir Espacio con borrado lógico/constraints.
- [x] `backend` Añadir Estancia mínima y unicidad activa.
- [x] `backend` Definir Horario y constraints definitivos.
- [x] `backend` Crear repositories base por módulo.
- [x] `backend` Versionar migraciones y corregir `.gitignore`.
- [x] `backend` Uniformar excepciones y prefijo `/api/v1/`.
- [x] `backend` Generar OpenAPI inicial.
- [x] `tests` Probar constraints, migración vacía y ruta de upgrade aprobada. **Evidencia:** 13 tests sobre PostgreSQL 16 y migración desde DB vacía.
- [x] `docs` Actualizar modelo y contratos con decisiones finales.

Evidencia detallada: `specs/002-modelos-migraciones-contratos/evidence.md`.

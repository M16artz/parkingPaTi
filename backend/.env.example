# Plantilla de variables de entorno del backend.
# Copiar como .env y completar con valores reales (NUNCA versionar el .env real).

DJANGO_SETTINGS_MODULE=config.settings.development
DJANGO_SECRET_KEY=cambia-esto-por-una-clave-secreta-larga-y-aleatoria
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=parkingpati
DB_USER=parkingpati
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Google Drive (storage_backends.py)
GOOGLE_DRIVE_CREDENTIALS_PATH=/ruta/a/credenciales.json
GOOGLE_DRIVE_FOLDER_ID=
